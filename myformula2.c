#include <stdio.h>

int main()
{
    int a;
    int b;
    int c;
    int d;
    int e;
    scanf("%d,%d,%d,%d,%d", &a, &b, &c, &d, &e);

    printf("all are not in neither assending order nor decending order");

    return 0;
}